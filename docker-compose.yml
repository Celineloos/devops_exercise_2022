version: '3.9'

services:
    # api:
    #     build: "./api"
    #     container_name: api
    #     hostname: api
    #     ports:
    #         - "3000:3000"
    #     environment:
    #         - MONGO_URL=mongodb://root:example@database:27017/devops_db
    #     volumes:
    #         - ./api:/app

    frontend:
        build: ./frontend
        ports:
        - '4200:4200'
            
    # poort vanaf buiten de docker-compose niet te benaderen?
    # database:
    #     image: mongo:latest
    #     container_name: mongodb
    #     restart: always
    #     environment:
    #         MONGO_INITDB_ROOT_USERNAME: root
    #         MONGO_INITDB_ROOT_PASSWORD: example
    #         MONGO_INITDB_DATABASE: devops_db
    #     volumes:
    #         - ./database/mongo-init.js:/docker-entrypoint-initdb.d/init.js:ro

    # nginx:
    #     build: "./"
    #     container_name: ginx
    #     restart: always
    #     ports:
    #     - "80:80"
    #     links:
    #     - frontend
    # mongo-express:
    #     image: mongo-express
    #     restart: always
    #     ports:
    #     - 8081:8081
    #     environment:
    #     ME_CONFIG_MONGODB_ADMINUSERNAME: root
    #     ME_CONFIG_MONGODB_ADMINPASSWORD: example
    #     ME_CONFIG_MONGODB_URL: mongodb://root:example@mongodb:27017/